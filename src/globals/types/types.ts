export type ExploreData = {
  img: string;
  location: string;
  distance: string;
};

export type CardsData = {
  img: string;
  title: string;
};

export type SearchPageData = {
  img: string;
  location: string;
  title: string;
  description: string;
  star: number;
  price: string;
  total: string;
  long: number;
  lat: number;
};
